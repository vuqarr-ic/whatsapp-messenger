# Пошаговая инструкция: Развертывание STORK на российском хостинге

## 🎯 Цель
Развернуть бэкенд (signaling сервер) и фронтенд на российском хостинге для стабильной работы без VPN.

## 📋 Что нужно:
1. Аккаунт на российском хостинге (Timeweb, Selectel, REG.RU, Beget)
2. Домен (можно использовать бесплатный поддомен от хостинга)
3. Базовые знания Linux (или следуйте инструкции шаг за шагом)

---

## Вариант 1: Timeweb (рекомендуется для начинающих)

### Шаг 1: Регистрация и создание VPS

1. Зайдите на https://timeweb.com
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "VPS"
4. Создайте новый VPS:
   - **ОС:** Ubuntu 22.04 LTS
   - **Конфигурация:** Минимум 1 CPU, 1GB RAM (достаточно для начала)
   - **Тариф:** от 200₽/месяц
5. Дождитесь создания сервера (5-10 минут)
6. Запишите IP адрес сервера и пароль root

### Шаг 2: Подключение к серверу

**Windows:**
1. Скачайте PuTTY: https://www.putty.org/
2. Запустите PuTTY
3. Введите IP адрес сервера
4. Port: 22
5. Connection type: SSH
6. Нажмите "Open"
7. Введите логин: `root`
8. Введите пароль (при вводе пароль не отображается - это нормально)

**Mac/Linux:**
```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 3: Обновление системы

```bash
apt update && apt upgrade -y
```

### Шаг 4: Установка Node.js

```bash
# Установка Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Проверка установки
node --version
npm --version
```

Должно показать версии Node.js и npm.

### Шаг 5: Установка Git

```bash
apt-get install -y git
```

### Шаг 6: Загрузка кода на сервер

**Вариант A: Через Git (если код на GitHub)**

```bash
cd /root
git clone https://github.com/ВАШ_НИК/stork-messenger.git
cd stork-messenger
```

**Вариант B: Через SCP (если код локально)**

На вашем компьютере (Windows PowerShell или Mac/Linux терминал):

```bash
# Создайте архив проекта
cd c:\Users\masimov\whatsapp-messenger
tar -czf stork.tar.gz --exclude=node_modules --exclude=.git .

# Загрузите на сервер
scp stork.tar.gz root@ВАШ_IP:/root/

# На сервере распакуйте
ssh root@ВАШ_IP
cd /root
tar -xzf stork.tar.gz
cd whatsapp-messenger
```

### Шаг 7: Установка зависимостей

```bash
cd /root/whatsapp-messenger  # или /root/stork-messenger
npm install
```

### Шаг 8: Установка PM2 (для автозапуска)

```bash
npm install -g pm2
```

### Шаг 9: Запуск signaling сервера

```bash
cd /root/whatsapp-messenger/server
pm2 start index.js --name stork-signaling
pm2 save
pm2 startup
```

PM2 покажет команду для автозапуска - выполните её.

### Шаг 10: Проверка работы сервера

```bash
pm2 status
pm2 logs stork-signaling
```

Должно показать, что сервер запущен и работает на порту 3005.

### Шаг 11: Установка и настройка Nginx

```bash
apt-get install -y nginx
```

Создайте конфигурационный файл:

```bash
nano /etc/nginx/sites-available/stork
```

Вставьте следующее (замените `ВАШ_ДОМЕН.ru` на ваш домен):

```nginx
# WebSocket прокси для signaling сервера
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name ВАШ_ДОМЕН.ru;

    # WebSocket для signaling сервера
    location / {
        proxy_pass http://localhost:3005;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для WebSocket
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

Включите сайт:

```bash
ln -s /etc/nginx/sites-available/stork /etc/nginx/sites-enabled/
rm /etc/nginx/sites-enabled/default  # Удаляем дефолтный сайт
nginx -t  # Проверка конфигурации
systemctl restart nginx
```

### Шаг 12: Настройка домена

1. В панели управления Timeweb найдите раздел "Домены"
2. Добавьте домен или используйте поддомен (например, `stork.yourdomain.ru`)
3. Настройте A-запись, указывающую на IP вашего VPS

### Шаг 13: Установка SSL (Let's Encrypt)

```bash
apt-get install -y certbot python3-certbot-nginx
certbot --nginx -d ВАШ_ДОМЕН.ru
```

Следуйте инструкциям. Certbot автоматически настроит SSL.

### Шаг 14: Развертывание фронтенда

**Вариант A: На том же сервере**

```bash
cd /root/whatsapp-messenger
npm run build
```

Создайте конфигурацию Nginx для статики:

```bash
nano /etc/nginx/sites-available/stork-frontend
```

```nginx
server {
    listen 80;
    server_name app.ВАШ_ДОМЕН.ru;  # или другой поддомен

    root /root/whatsapp-messenger/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

Включите:

```bash
ln -s /etc/nginx/sites-available/stork-frontend /etc/nginx/sites-enabled/
certbot --nginx -d app.ВАШ_ДОМЕН.ru
systemctl restart nginx
```

**Вариант B: На отдельном статическом хостинге**

1. Соберите проект локально: `npm run build`
2. Загрузите папку `dist` на статический хостинг (Timeweb, REG.RU и т.д.)
3. Настройте переменные окружения (см. ниже)

### Шаг 15: Настройка переменных окружения

**Для фронтенда:**

Если фронтенд на том же сервере, создайте файл `.env` в корне проекта:

```bash
nano /root/whatsapp-messenger/.env
```

Добавьте:

```env
VITE_SIGNALING_URL=wss://ВАШ_ДОМЕН.ru
VITE_USE_WEBRTC=false
```

Пересоберите:

```bash
cd /root/whatsapp-messenger
npm run build
```

Если фронтенд на Netlify/Vercel, добавьте переменные в настройках проекта:
- `VITE_SIGNALING_URL` = `wss://ваш-домен.ru`
- `VITE_USE_WEBRTC` = `false`

### Шаг 16: Проверка работы

1. Откройте в браузере: `https://app.ВАШ_ДОМЕН.ru` (или ваш URL фронтенда)
2. Откройте консоль браузера (F12)
3. Должно быть: `P2P Service initialized, Peer ID: ... (Signaling only)`
4. Проверьте WebSocket соединение - должно быть установлено

---

## Вариант 2: Selectel

Процесс аналогичен Timeweb, но:
- Используйте панель управления Selectel
- VPS создается через панель
- Остальные шаги идентичны

---

## Вариант 3: REG.RU

1. Зарегистрируйтесь на https://reg.ru
2. Создайте VPS через панель управления
3. Следуйте шагам 2-16 из инструкции Timeweb

---

## 🔧 Полезные команды

### Проверка статуса сервера:
```bash
pm2 status
pm2 logs stork-signaling
```

### Перезапуск сервера:
```bash
pm2 restart stork-signaling
```

### Остановка сервера:
```bash
pm2 stop stork-signaling
```

### Проверка портов:
```bash
netstat -tulpn | grep 3005
```

### Проверка Nginx:
```bash
nginx -t
systemctl status nginx
```

### Просмотр логов:
```bash
pm2 logs stork-signaling --lines 50
tail -f /var/log/nginx/error.log
```

---

## 🐛 Решение проблем

### Проблема: Сервер не запускается

```bash
# Проверьте логи
pm2 logs stork-signaling

# Проверьте, занят ли порт
netstat -tulpn | grep 3005

# Если порт занят, измените PORT в server/index.js или через переменную окружения
PORT=3006 pm2 restart stork-signaling
```

### Проблема: WebSocket не подключается

1. Проверьте firewall:
```bash
ufw allow 80
ufw allow 443
ufw allow 22
ufw enable
```

2. Проверьте конфигурацию Nginx:
```bash
nginx -t
```

3. Проверьте логи Nginx:
```bash
tail -f /var/log/nginx/error.log
```

### Проблема: SSL не работает

```bash
# Переустановите SSL
certbot --nginx -d ВАШ_ДОМЕН.ru --force-renewal
```

---

## 📝 Чеклист развертывания

- [ ] Создан VPS на российском хостинге
- [ ] Установлен Node.js
- [ ] Загружен код на сервер
- [ ] Установлены зависимости (`npm install`)
- [ ] Запущен signaling сервер через PM2
- [ ] Настроен Nginx для WebSocket
- [ ] Настроен домен
- [ ] Установлен SSL (Let's Encrypt)
- [ ] Развернут фронтенд
- [ ] Настроены переменные окружения
- [ ] Протестирована работа

---

## 💡 Рекомендации

1. **Для начала:** Используйте только signaling (`VITE_USE_WEBRTC=false`) - это проще и стабильнее
2. **Мониторинг:** Настройте мониторинг PM2: `pm2 monit`
3. **Резервное копирование:** Регулярно делайте бэкапы кода
4. **Обновления:** Регулярно обновляйте систему: `apt update && apt upgrade`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs stork-signaling`
2. Проверьте конфигурацию Nginx: `nginx -t`
3. Проверьте firewall: `ufw status`
4. Убедитесь, что домен указывает на правильный IP

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать на российском хостинге без VPN и блокировок!

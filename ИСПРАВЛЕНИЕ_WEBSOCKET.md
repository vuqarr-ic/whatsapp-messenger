# 🔧 Исправление ошибки WebSocket подключения

## Проблема
```
WebSocket connection to 'wss://whatsapp-messenger-h0sl.onrender.com/' failed: 
WebSocket opening handshake timed out
```

## ✅ Что уже исправлено в коде

1. ✅ Сервер теперь слушает на `0.0.0.0` (доступен извне)
2. ✅ Добавлена обработка HTTP запросов для проверки здоровья
3. ✅ URL нормализуется (убирается слэш в конце)

## 🔍 Что нужно проверить на Render

### Шаг 1: Проверьте статус сервера на Render

1. Зайдите на https://render.com
2. Откройте ваш сервис `whatsapp-messenger-h0sl`
3. Проверьте **"Logs"** (логи):
   - Должна быть строка: `Signaling server: ws://0.0.0.0:XXXX`
   - Если видите ошибки - скопируйте их

### Шаг 2: Проверьте URL в Netlify

1. Зайдите на https://app.netlify.com
2. Откройте ваш сайт
3. Перейдите в **Site settings → Environment variables**
4. Проверьте переменную `VITE_SIGNALING_URL`:
   - ✅ Правильно: `wss://whatsapp-messenger-h0sl.onrender.com` (БЕЗ слэша в конце)
   - ❌ Неправильно: `wss://whatsapp-messenger-h0sl.onrender.com/` (со слэшем)

### Шаг 3: Исправьте URL, если нужно

Если в Netlify URL со слэшем в конце:

1. В Netlify: **Site settings → Environment variables**
2. Найдите `VITE_SIGNALING_URL`
3. Нажмите **"Edit"**
4. Уберите слэш в конце: `wss://whatsapp-messenger-h0sl.onrender.com`
5. Нажмите **"Save"**
6. Перейдите в **Deploys** → нажмите **"Trigger deploy"** → **"Deploy site"**

### Шаг 4: Проверьте, что сервер работает

Откройте в браузере:
```
https://whatsapp-messenger-h0sl.onrender.com/health
```

Должно показать:
```json
{"status":"ok","service":"stork-signaling"}
```

Если показывает ошибку или таймаут - сервер на Render не запущен или не работает.

### Шаг 5: Проверьте настройки Render

1. В Render откройте ваш сервис
2. Проверьте **Settings**:
   - **Start Command**: должно быть `node server/index.js`
   - **Environment**: переменная `PORT` не обязательна (Render сам назначит)

3. Если сервер не запускается:
   - Проверьте **Logs** на ошибки
   - Убедитесь, что в репозитории есть файл `server/index.js`
   - Убедитесь, что в `package.json` есть зависимость `ws`

## 🔄 После исправления

1. **Пересоберите проект на Netlify:**
   - Deploys → Trigger deploy → Deploy site

2. **Перезапустите сервер на Render** (если нужно):
   - Manual Deploy → Clear build cache & deploy

3. **Проверьте в браузере:**
   - Откройте ваш сайт Netlify
   - Откройте консоль (F12)
   - Не должно быть ошибок WebSocket
   - Должно быть: `P2P Service initialized, Peer ID: ... (WebRTC)`

## ❓ Если всё ещё не работает

### Проверка 1: Сервер на Render

Откройте в браузере:
```
https://whatsapp-messenger-h0sl.onrender.com/health
```

- ✅ Работает → переходите к Проверке 2
- ❌ Не работает → проблема на Render, проверьте логи

### Проверка 2: WebSocket подключение

В консоли браузера (F12) проверьте:
- Есть ли ошибки WebSocket?
- Какой URL используется? (должен быть БЕЗ слэша в конце)

### Проверка 3: Переменные окружения

В Netlify проверьте:
- `VITE_SIGNALING_URL` = `wss://whatsapp-messenger-h0sl.onrender.com` (без слэша)
- После изменения переменной нужно пересобрать проект

## 📝 Быстрое решение

Если ничего не помогает:

1. **В Netlify:**
   - Environment variables → `VITE_SIGNALING_URL`
   - Установите: `wss://whatsapp-messenger-h0sl.onrender.com` (без слэша)
   - Сохраните
   - Trigger deploy

2. **В Render:**
   - Проверьте логи
   - Если есть ошибки - пересоздайте сервис

3. **Проверьте:**
   - Откройте `https://whatsapp-messenger-h0sl.onrender.com/health`
   - Должно показать `{"status":"ok"}`

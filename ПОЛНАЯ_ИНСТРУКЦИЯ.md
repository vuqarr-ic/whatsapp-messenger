# 📘 ПОЛНАЯ ИНСТРУКЦИЯ: Как сделать STORK доступным для всех

## 🎯 Цель
Разместить мессенджер STORK в интернете, чтобы им могли пользоваться все.

---

## ЧАСТЬ 1: Подготовка (у вас уже есть проект)

✅ У вас уже есть папка: `C:\Users\masimov\whatsapp-messenger`  
✅ Проект уже работает локально

**Что дальше:** Нужно загрузить проект на GitHub, затем разместить на хостингах.

---

## ЧАСТЬ 2: Загрузка проекта на GitHub

### Вариант A: С Git (рекомендуется, но нужно установить)

#### Шаг 1: Установите Git

1. Откройте: https://git-scm.com/download/win
2. Скачайте установщик (автоматически определит вашу систему)
3. Запустите установщик
4. Всё время нажимайте **"Next"** (настройки по умолчанию подойдут)
5. В конце установки выберите: **"Git from the command line and also from 3rd-party software"**
6. Завершите установку

#### Шаг 2: Перезапустите PowerShell

Закройте текущее окно PowerShell и откройте новое.

#### Шаг 3: Проверьте установку

```bash
git --version
```

Должно показать версию (например: `git version 2.43.0`)

#### Шаг 4: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Войдите (или зарегистрируйтесь)
3. Нажмите зелёную кнопку **"New"** (или плюсик → "New repository")
4. Заполните:
   - **Repository name**: `stork-messenger`
   - **Description**: `STORK - P2P мессенджер`
   - Выберите **Public**
   - **НЕ** ставьте галочки на "Add README" и т.п.
5. Нажмите **"Create repository"**

#### Шаг 5: Загрузите код

В PowerShell выполните (замените `ВАШ_НИК` на ваш GitHub username):

```bash
cd C:\Users\masimov\whatsapp-messenger

git init
git add .
git commit -m "Initial STORK version"
git branch -M main
git remote add origin https://github.com/ВАШ_НИК/stork-messenger.git
git push -u origin main
```

При первом `git push` GitHub попросит авторизоваться:
- Введите ваш GitHub username
- Введите пароль (или Personal Access Token, если включена 2FA)

---

### Вариант B: БЕЗ Git (через веб-интерфейс)

Если не хотите устанавливать Git, используйте веб-интерфейс GitHub:

1. **Создайте репозиторий на GitHub** (как в Варианте A, шаг 4)

2. **На странице репозитория** нажмите **"uploading an existing file"**

3. **Откройте папку проекта** в проводнике:
   ```
   C:\Users\masimov\whatsapp-messenger
   ```

4. **Выделите ВСЕ файлы** (Ctrl+A) и **перетащите** их в окно браузера на GitHub

5. **Внизу страницы** введите: `Initial STORK version`

6. **Нажмите** "Commit changes"

7. **Готово!** Обновите страницу - должны увидеть все файлы

---

## ЧАСТЬ 3: Размещение сервера сигналов на Render

### Шаг 1: Регистрация на Render

1. Зайдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через **GitHub** (удобнее всего)

### Шаг 2: Создайте Web Service

1. В панели Render нажмите **"New +"** → **"Web Service"**

2. **Подключите GitHub:**
   - Если первый раз: нажмите "Connect GitHub" и дайте доступ
   - Выберите репозиторий: `stork-messenger`

3. **Настройки сервиса:**
   - **Name**: `stork-signaling` (или любое имя)
   - **Region**: выберите ближайший (например, Frankfurt)
   - **Branch**: `main`
   - **Runtime**: `Node`
   - **Build Command**: оставьте **пустым**
   - **Start Command**: 
     ```
     node server/index.js
     ```

4. **Environment Variables (переменные окружения):**
   - Нажмите "Advanced"
   - Добавьте переменную:
     - **Key**: `PORT`
     - **Value**: `3005`
   - (Можно оставить пустым - Render сам назначит порт)

5. **Нажмите** "Create Web Service"

6. **Дождитесь деплоя** (2-3 минуты)

### Шаг 3: Получите URL сервера

После успешного деплоя Render покажет URL вида:
```
https://stork-signaling.onrender.com
```

**Запомните этот URL!** Для WebSocket используйте `wss://`:
```
wss://stork-signaling.onrender.com
```

Это ваш **VITE_SIGNALING_URL**.

---

## ЧАСТЬ 4: Размещение клиента на Netlify

### Шаг 1: Регистрация на Netlify

1. Зайдите на https://www.netlify.com/
2. Нажмите **"Sign up"**
3. Выберите **"Sign up with GitHub"** (удобнее всего)

### Шаг 2: Создайте сайт

1. В панели Netlify нажмите **"Add new site"** → **"Import from Git"**

2. **Выберите GitHub** и дайте доступ к репозиторию

3. **Выберите репозиторий**: `stork-messenger`

4. **Настройки сборки:**
   - **Branch to deploy**: `main`
   - **Build command**: 
     ```
     npm run build
     ```
   - **Publish directory**: 
     ```
     dist
     ```

5. **Environment variables (переменные окружения):**
   - Нажмите "Show advanced"
   - Нажмите "New variable"
   - Добавьте:
     - **Key**: `VITE_SIGNALING_URL`
     - **Value**: `wss://stork-signaling.onrender.com` (ваш URL с Render, обязательно `wss://`)

6. **Нажмите** "Deploy site"

7. **Дождитесь сборки** (2-3 минуты)

### Шаг 3: Получите URL приложения

После успешного деплоя Netlify покажет URL вида:
```
https://stork-app-123456.netlify.app
```

**Это и есть адрес вашего мессенджера!** Раздавайте его всем.

---

## ЧАСТЬ 5: Проверка работы

### Тест 1: Откройте на компьютере

1. Откройте в браузере: `https://ваш-сайт.netlify.app`
2. Должно загрузиться приложение STORK
3. Откройте консоль браузера (F12) - не должно быть ошибок WebSocket

### Тест 2: Откройте на телефоне

1. Откройте тот же URL на телефоне (через мобильный интернет, не Wi-Fi)
2. Приложение должно загрузиться
3. Создайте чат между компьютером и телефоном
4. Отправьте сообщение - должно работать!

---

## ЧАСТЬ 6: Что делать дальше

### Для пользователей (инструкция)

Создайте файл `USER_GUIDE.md` с инструкцией для пользователей (см. ниже).

### Обновление проекта

Если вы внесли изменения в код:

**С Git:**
```bash
cd C:\Users\masimov\whatsapp-messenger
git add .
git commit -m "Описание изменений"
git push
```

Render и Netlify автоматически пересоберут проект.

**Без Git:**
- Загрузите изменённые файлы через веб-интерфейс GitHub
- Render и Netlify автоматически пересоберут проект

---

## ❓ Частые проблемы

### Проблема: "Git не является командой"
**Решение:** Установите Git (см. Вариант A выше) или используйте веб-интерфейс (Вариант B)

### Проблема: Render не может подключиться к GitHub
**Решение:** Проверьте, что репозиторий **Public**, и вы дали Render доступ к GitHub

### Проблема: Netlify показывает ошибку сборки
**Решение:** 
- Проверьте, что в репозитории есть `package.json`
- Проверьте, что `VITE_SIGNALING_URL` указан правильно (с `wss://`)

### Проблема: WebSocket не подключается
**Решение:**
- Проверьте, что сервер на Render запущен (зелёный статус)
- Проверьте, что в Netlify указан правильный `VITE_SIGNALING_URL` (с `wss://`, не `ws://`)

---

## ✅ Готово!

После выполнения всех шагов ваш мессенджер будет доступен по адресу Netlify для всех пользователей интернета!
